<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" rel="stylesheet" />

    <style>
        body {
            padding-top: 60px;
            background-color: #0b0c10;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .navbar {
            margin-bottom: 40px;
        }
        .character {
            margin-bottom: 40px;
        }
        img {
            max-width: 100%;
            border-radius: 10px;
            margin-bottom: 15px;
        }
        footer {
            margin-top: 60px;
            padding: 20px 0;
            text-align: center;
            border-top: 1px solid #444;
            font-size: 0.9rem;
            color: #aaa;
        }
        a.nav-link.active {
            font-weight: bold;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">Star Wars</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link {% if current_url starts with '/jedi' %}active{% endif %}" href="/jedi">Джедаи</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if current_url starts with '/sith' %}active{% endif %}" href="/sith">Ситхи</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container">
        {% block content %}
            <p class="text-muted">Пусто...</p>
        {% endblock %}
    </main>

    <footer>
        <div class="container">
            &copy; {{ "now"|date("Y") }} Star Wars Galaxy | Сайт создан для учебных целей
        </div>
    </footer>

</body>
</html>
